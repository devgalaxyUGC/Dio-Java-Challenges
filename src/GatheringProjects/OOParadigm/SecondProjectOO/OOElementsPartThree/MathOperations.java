package GatheringProjects.OOParadigm.SecondProjectOO.OOElementsPartThree;

interface MathOperations {

    // Notice that each method doesn't have any scope, just signs
    void sumNum(int firstOp, int scdOp);

    void subNum(int firstOp, int scdOp);

    void mutNum(int firstOp, int scdOp);

    void divNum(int firstOp, int scdOp);

}
